{
  "name": "chaoxing-api",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "超星学习通小组网盘 API 服务",
  "main": "src/index.js",
  "dependencies": {
    "axios": "^1.7.2",
    "bcrypt": "^6.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "jsonwebtoken": "^9.0.2",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "nedb": "^1.8.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "terminal": "node src/index.js --terminal",
    "build": "echo 'No build step required for Node.js'",
    "deploy": "npm install && npm start",
    "heroku": "npm install && npm start",
    "vercel": "npm install && npm run build",
    "docker": "docker build -t chaoxing-api . && docker run -p 3000:3000 chaoxing-api"
  }
}
